<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.burger.menu.model.MenuOptionDAO">
	
	<select id="sideList" parameterType="String" resultType="com.burger.menu.model.MenuOptionDTO">
		select * from menu_option where op_cat = #{op_cat}
	</select>
	
</mapper>